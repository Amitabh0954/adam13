Given the user story for profile management, we need to allow users to update their personal information and preferences, saving the changes correctly and ensuring that any updates reflect immediately. This will be addressed under the `user_management/profile` directory.

Here’s the implementation plan:

1. Update the `User` model to include profile information fields.
2. Create API endpoints for retrieving and updating the user's profile.
3. Update the SQL schema to handle new fields for profile data.

We will sequentially generate these files: