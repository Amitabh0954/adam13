# Generating files as per the provided folder structure and user stories

# Epic title
Epic_Title = "User Account Management System"

# In-memory ledger initialization
memory_ledger = {
    "emitted_file_paths": [],
    "declared_entities": [],
    "public_interfaces": [],
    "imports": [],
    "folder_file_mapping": {},
    "persistent_fields": {},
    "folder_responsibilities": {}
}

# Existing information
memory_ledger["declared_entities"] = ["User", "Session"]
memory_ledger["public_interfaces"] = ["UserRepository", "RegistrationService", "SessionRepository", "LoginService"]
memory_ledger["emitted_file_paths"] = [
    "backend/user_account_management/models/user.py",
    "backend/user_account_management/models/session.py",
    "backend/user_account_management/repositories/user_repository.py",
    "backend/user_account_management/repositories/session_repository.py",
    "backend/user_account_management/services/registration_service.py",
    "backend/user_account_management/services/login_service.py",
    "app.py"
]

memory_ledger["imports"] = [
    "backend/user_account_management/models/user:User",
    "backend/user_account_management/models/session:Session as SessionModel",
    "backend/user_account_management/repositories/user_repository:UserRepository",
    "backend/user_account_management/repositories/session_repository:SessionRepository"
]

memory_ledger["folder_file_mapping"] = {
    "backend/user_account_management/models/": ["user.py", "session.py"],
    "backend/user_account_management/repositories/": ["user_repository.py", "session_repository.py"],
    "backend/user_account_management/services/": ["registration_service.py", "login_service.py"]
}

# Persistent fields and responsibilities
memory_ledger["persistent_fields"] = {
    "User": ["id", "email", "password", "is_active"],
    "Session": ["id", "user_id", "session_token", "created_at", "is_active"]
}

memory_ledger["folder_responsibilities"] = {
    "backend/user_account_management/models/": "Models for user account management",
    "backend/user_account_management/repositories/": "Repositories for user account management",
    "backend/user_account_management/services/": "Services for user account management"
}

# Folder: backend/user_account_management/models/
# File: password_reset.py
file_content = 
# Epic Title: User Account Management System

from sqlalchemy import Column, Integer, String, DateTime, ForeignKey
from sqlalchemy.ext.declarative import declarative_base
from datetime import datetime, timedelta

Base = declarative_base()

class PasswordReset(Base):
    __tablename__ = "password_resets"

    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, ForeignKey("users.id"), nullable=False)
    reset_token = Column(String(255), unique=True, nullable=False)
    expires_at = Column(DateTime, nullable=False)

    def __init__(self, user_id: int, reset_token: str):
        self.user_id = user_id
        self.reset_token = reset_token
        self.expires_at = datetime.utcnow() + timedelta(hours=24)

    def __repr__(self) -> str:
        return f"<PasswordReset(user_id={self.user_id}, reset_token={self.reset_token}, expires_at={self.expires_at})>"


memory_ledger["emitted_file_paths"].append("backend/user_account_management/models/password_reset.py")
memory_ledger["declared_entities"].append("PasswordReset")
memory_ledger["folder_file_mapping"]["backend/user_account_management/models/"].append("password_reset.py")

print("