To address the user story for handling user login and security measures like limiting invalid login attempts and session timeout after inactivity, we need to add relevant functionality in the `authentication` and `session` management areas.

We will continue building on the existing structure under the `authentication` modules and add the necessary logic for user login.