To handle password recovery including email verification and expiring reset links, we need to extend the authentication module with relevant functionality.

We will continue building on the existing structure under the `authentication` modules and add the necessary logic for password recovery.