flask==2.1.1
sqlalchemy==1.4.27
marshmallow==3.14.1
pymysql==1.0.2
werkzeug==2.0.2

### Summary

This implementation of user registration ensures that:
- Email is unique.
- Password meets security criteria (at least 8 characters, contains a letter, and a number).
- API endpoints are provided to register a new user.
- The MySQL database schema supports necessary constraints for user management.
- The application initializes properly with the new feature and necessary dependencies.

This implementation aligns with the user stories and provides a production-ready solution for user registration functionality.